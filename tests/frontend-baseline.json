{
  "_comment": "Empty on purpose: the lane is fully green. The handoff work that introduced this runner also fixed the six pre-existing failures it exposed (group-gateway, group-room, chat-ui-contract, session-view-contract, sessions-agents-header-contract, chat-handoff-contract). An entry here is a debt item: the runner FAILS if a listed file starts passing, so a stale entry cannot outlive its bug. Add one only for a genuinely pre-existing failure you are not fixing yet, with the reason.",
  "known_failing": {}
}
