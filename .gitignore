node_modules
dist
.DS_Store
__pycache__/
*.pyc
.pytest_cache/
.env
.env.*
!.env.example

# Push device subscriptions (local state, not source)
server/push_subscriptions.json

# Runtime state files
server/last_chat.json
server/chat_handoffs.json
server/chat_titles.json
server/room_vaults.json
server/whiteboard_state.json

# External plugin UI symlinks — generated locally by scripts/setup-plugins.sh
src/plugins/*
server/room_tools.json
server/last_room.json

# Working plans are personal scratch: they name real bots, profiles and vaults.
# They live in ~/.hermes/plans/ and are never part of the product.
.hermes/plans/
tsconfig.tsbuildinfo
